#include <bits/stdc++.h>

using namespace std;

void solve(){
    vector<int> fact;
    vector<int> cnt(26);

    string s;
    cin >> s;

    for (char c:s){

    }
}


int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout<<fixed<<setprecision(0);

    solve();

    return 0;
}
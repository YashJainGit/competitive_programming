#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> adj;
vector<bool> v;
int dfs(int i){

}
void solve(){
    for (int i)
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout<<fixed<<setprecision(0);
    solve();
    return 0;
}